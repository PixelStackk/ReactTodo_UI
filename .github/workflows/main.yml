name: React Todo CI/CD

on:
  push:
    branches:
      - main
      - feature

jobs:
  build:
    runs-on: Todo_Runner

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 16

      - run: npm install

      - run: npm run build
